{
  "name": "event-service",
  "version": "1.0.0",
  "description": "Event Service - Separate service for calendar APIs",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
